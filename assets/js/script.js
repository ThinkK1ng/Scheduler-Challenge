// This Display's today's day and date.
var currentDate = moment().format('dddd,MMM Do YYYY')
$("currentDay").html(currentDate);

$(document).ready(function () {
    //saveBtn click listener here
    $(".saveBtn").on("click", funtion() {
        var text=
    })
})